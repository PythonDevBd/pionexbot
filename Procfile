# Procfile for Pionex Trading Bot GUI
# This file specifies how to run the application

# Web process - Main Flask application
web: python main.py

# Alternative commands for different deployment scenarios:
# web: gunicorn --worker-class eventlet -w 1 --bind 0.0.0.0:$PORT main:app
# web: python -m flask run --host=0.0.0.0 --port=$PORT
# web: python gui_app.py

# Worker process (if needed for background tasks)
# worker: python worker.py

# Clock process (if needed for scheduled tasks)
# clock: python scheduler.py 